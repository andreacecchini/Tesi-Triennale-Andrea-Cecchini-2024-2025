(def connect (src dest)
  (let* ((d (distance-to dest))
	 (thepath (spath src dest d)))
	(probe d 0)
    (if thepath
       (let ((min-d (min-hood (nbr d))))
	 (sum-hood 
	  (mux (= (nbr d) min-d)
	    (nbr-vec)
	    (tup 0 0 0))))
      (tup 0 0 0)))
  )
